# Flask Configuration
SECRET_KEY=your-secret-key-here-generate-with-secrets-token-hex-32

# Google Gemini AI Configuration
GEMINI_API_KEY=your-gemini-api-key-here

# Database Configuration (Optional - for future use)
DATABASE_URL=sqlite:///tuvi.db

# Application Environment
FLASK_ENV=development
FLASK_DEBUG=True

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual configuration
# 3. Generate SECRET_KEY with: python -c "import secrets; print(secrets.token_hex(32))"
# 4. Get GEMINI_API_KEY from: https://makersuite.google.com/app/apikey
# 5. Never commit .env file to version control
